import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.docutain.sdk_example_capacitor_ionic',
  appName: 'docutain-sdk-example-capacitor-ionic',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
